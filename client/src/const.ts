export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// This app authenticates with store/CEO PINs (see PinKeypad + auth.verifyPin).
// The template's OAuth login-URL helper was removed as dead code.
